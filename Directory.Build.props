<Project>
	<PropertyGroup Condition="'$(FOMServerBuildEnvironment)' == 'Docker'">
		<!-- Make sure we're putting build outputs in the mounted volume. -->
		<OutputPath>/out/dotnet/$(MSBuildProjectName)/$(Configuration)/</OutputPath>
	</PropertyGroup>
</Project>
